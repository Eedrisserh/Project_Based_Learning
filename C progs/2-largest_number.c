#include <stdio.h>

/**
 * largest_number - returns the largest of 3 numbers
 * @a: first integer
 * @b: second integer
 * @c: third integer
 * Return: largest number
 */

int main(void)
{
    int a = 5;
    int b = '5';
    
    if (a != b)
    {
	printf("A is not equal to B, (a != b)\n");
	}
	
	if (a ! ==b)
	{
		printf(" NOT A EQUALS TO B");
	}
}