#include <ctype.h>
#include "main.h"
/** main - Prints all alphabets from a - z in lower case
 * @alpha: is the variable holding the alphabets
 * Return: On success the program returs 0 (65 - 90, 97 - 122)
 */

 int main(void)
{
	print_square(3);
       return (0);
}